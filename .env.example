# Base URL for the backend API (used by src/services/api.ts).
# Point this at your API server when you move the data behind HTTP endpoints.
# e.g. VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
